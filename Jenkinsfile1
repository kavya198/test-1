pipeline {
    agent any
    tools{
        jdk 'java-11'
        maven 'maven'
    }
    stages{
        stage('git checkout'){
            steps{
                git branch: 'staging', url: 'https://github.com/kavya198/test-1.git'
            }
        }
        stage('compile'){
            steps{
                sh "mvn compile"
            }
        }      
    }  
        
}